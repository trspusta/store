import React from 'react'

const SearchBar = () => {
  return (
    <div>
        <input type="text" />
        <div>
            <input type="checkbox" />
            only show products in stock
        </div>
    </div>
  );
};

export default SearchBar;